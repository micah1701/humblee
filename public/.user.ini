# PHP ini overrides for CGI/FastCGI; see http://php.net/manual/en/configuration.changes.php#configuration.changes.cgi
# Note that PHP settings in .user.ini files are only applied to the directory where the file is located and its subdirectories.
# For non-CGI/FastCGI setups, you may need to use .htaccess files or other methods to override PHP settings.

upload_max_filesize = 50M
post_max_size = 52M