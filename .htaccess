# Turn on URL rewriting
RewriteEngine On

# Protect hidden files from being viewed
<Files .*>
	Order Deny,Allow
	Deny From All
</Files>

# Turn of Indexes
Options -Indexes

RewriteRule ^(.*)$ public/$1 [L]